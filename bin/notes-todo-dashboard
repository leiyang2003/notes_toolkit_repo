#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -P "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=bin/_bootstrap.sh
source "$SCRIPT_DIR/_bootstrap.sh"

TOOL_ROOT="$(notes_toolkit_root)"
exec python3 "$TOOL_ROOT/notes_todo_dashboard.py" "$@"
